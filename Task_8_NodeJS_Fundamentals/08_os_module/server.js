import os from "os";

// userInfo()
console.log(os.userInfo()); //gives the user info

// totalmem()
console.log(os.totalmem()); // gives the amount of total memory

// freemem()
console.log(os.freemem()); // gives the amount of free memory

// cpus()
console.log(os.cpus()); // gives CPU information for every core
