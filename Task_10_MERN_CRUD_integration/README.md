1. Inside task 10 folder
   a. npm init vite@latest
   b. name the folder client
   c. cd client
   d. npm i bootstrap axios react-router-dom

2. Inside task 10 folder create a folder named server

   a. npm init -y
   b. npm install express mongoose cors nodemon
   c. Add this inside package.json inside scripts "start": "nodemon index.js"
